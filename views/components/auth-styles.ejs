<style>
  body, html { height: 100%; margin: 0; }

  .auth-split { display: flex; min-height: 100vh; }
  .auth-panel {
    width: 100%; max-width: 420px; flex-shrink: 0;
    display: flex; flex-direction: column; justify-content: center;
    padding: 48px 40px;
    background: var(--theme-bg-card);
    border-right: 1px solid var(--theme-border);
    position: relative; z-index: 1;
  }
  .auth-image {
    flex: 1;
    background-position: center; background-size: cover; background-repeat: no-repeat;
  }

  @media (max-width: 768px) {
    .auth-split { position: relative; display: block; min-height: 100vh; }
    .auth-image {
      position: fixed; inset: 0; display: block; z-index: 0;
    }
    .auth-panel {
      position: relative; z-index: 1; max-width: 100%;
      border-right: none; padding: 40px 24px; min-height: 100vh;
      justify-content: center;
      background: color-mix(in srgb, var(--theme-bg-card) 95%, transparent);
    }
  }

  .auth-input {
    width: 100%; padding: 10px 14px; border-radius: 10px;
    border: 1px solid var(--theme-border-input); background: var(--theme-bg-input);
    font-size: 14px; color: var(--theme-text); outline: none;
    transition: border-color 0.15s, box-shadow 0.15s;
    box-sizing: border-box; font-family: inherit;
  }
  .auth-input:focus {
    border-color: var(--theme-accent); box-shadow: 0 0 0 3px var(--theme-accent-subtle);
  }
  .auth-input::placeholder { color: var(--theme-text-placeholder); }
  .auth-input.invalid { border-color: var(--theme-danger) !important; }

  .auth-label { display: block; font-size: 13px; font-weight: 500; color: var(--theme-text-muted); margin-bottom: 6px; }

  .auth-submit {
    width: 100%; padding: 11px; border-radius: 10px;
    background: var(--theme-btn-primary-bg); color: var(--theme-btn-primary-text);
    font-size: 14px; font-weight: 500;
    border: none; cursor: pointer; transition: background 0.15s;
    font-family: inherit; display: flex; align-items: center; justify-content: center; gap: 8px;
  }
  .auth-submit:hover:not(:disabled) { background: var(--theme-btn-primary-hover); }
  .auth-submit:disabled { opacity: 0.6; cursor: default; }

  .pw-wrapper { position: relative; }
  .pw-toggle {
    position: absolute; right: 12px; top: 50%; transform: translateY(-50%);
    background: none; border: none; cursor: pointer; padding: 0;
    color: var(--theme-text-muted); line-height: 0;
  }
  .pw-toggle:hover { color: var(--theme-text-strong); }

  .pw-strength { height: 3px; border-radius: 2px; background: var(--theme-bg-secondary); margin-top: 8px; overflow: hidden; }
  .pw-bar { height: 100%; border-radius: 2px; transform-origin: left center; transform: scaleX(0); transition: transform 0.28s ease, background 0.28s ease; }
  .pw-hint { font-size: 11px; color: var(--theme-text-muted); margin-top: 5px; min-height: 14px; transition: color 0.2s; }

  .cb-row { display: flex; align-items: center; gap: 9px; cursor: pointer; user-select: none; }
  .cb-box {
    width: 16px; height: 16px; border-radius: 4px; flex-shrink: 0;
    border: 1.5px solid var(--theme-border-input); background: var(--theme-bg-input);
    display: flex; align-items: center; justify-content: center;
    transition: background 0.12s, border-color 0.12s;
  }
  .cb-check { display: none; }

  .spinner {
    width: 15px; height: 15px; flex-shrink: 0;
    border: 2px solid color-mix(in srgb, var(--theme-btn-primary-text) 25%, transparent);
    border-top-color: var(--theme-btn-primary-text);
    border-radius: 50%; animation: spin 0.65s linear infinite; display: none;
  }
  .auth-submit.loading .spinner { display: block; }
  .auth-submit.loading .btn-label { display: none; }
  @keyframes spin { to { transform: rotate(360deg); } }
  @media (prefers-reduced-motion: reduce) { .spinner { animation: none; } }

  .auth-panel { opacity: 0; transform: translateX(-12px); transition: opacity 0.4s ease, transform 0.4s cubic-bezier(0.16,1,0.3,1); will-change: opacity, transform; }
  .auth-panel.visible { opacity: 1; transform: translateX(0); }
</style>
