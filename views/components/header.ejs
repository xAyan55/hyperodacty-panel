<!DOCTYPE html>
<html class="h-full js-loading" lang="<%= (req && req.lang) || 'en' %>">
<%
// Overview and Credits own their backgrounds — never show the panel
// wallpaper there so their existing UX is preserved unchanged.
const wallpaperExcluded = req && req.path && (
  req.path.startsWith('/admin/overview') ||
  req.path.startsWith('/credits')
);
%>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="theme-color" content="#0a0a0a" media="(prefers-color-scheme: dark)">
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)">
    <% if (typeof csrfToken !== 'undefined') { %>
    <meta name="csrf-token" content="<%= csrfToken %>">
    <% } %>
    <title><%= settings.title %> - <%= title %></title>
    <link rel="stylesheet" href="/styles.css?v=8">
    <link rel="stylesheet" href="/layout-animations.css?v=8">
    <link rel="stylesheet" href="/styles/motion.css?v=8">
    <link rel="icon" href="<%= settings.favicon && settings.favicon.startsWith('/') ? settings.favicon : '/favicon.ico' %>" type="image/x-icon">
    <link rel="shortcut icon" href="<%= settings.favicon && settings.favicon.startsWith('/') ? settings.favicon : '/favicon.ico' %>" type="image/x-icon">
    <!-- Default themes — always loaded, always the base layer -->
    <link rel="stylesheet" href="/themes/default-light.css">
    <link rel="stylesheet" href="/themes/default-dark.css">
    <!-- Optional override themes (solarized, material, user-defined) -->
    <% if (settings.lightTheme && settings.lightTheme !== 'default') { %>
    <link rel="stylesheet" href="<%= settings.lightTheme %>" id="light-theme-css" data-theme-mode="light">
    <% } %>
    <% if (settings.darkTheme && settings.darkTheme !== 'default') { %>
    <link rel="stylesheet" href="<%= settings.darkTheme %>" id="dark-theme-css" data-theme-mode="dark">
    <% } %>
    <link rel="stylesheet" href="/fonts/general-sans/general-sans.css">
    <script nonce="<%- nonce %>" defer src="/js/csrf.js?v=8"></script>
    <script nonce="<%- nonce %>" defer src="/js/shared/al-icon.js?v=8"></script>
    <script nonce="<%- nonce %>">window.__sessionExpiredMsg = '<%= (req.translations && req.translations.sessionExpired) || "Your session expired. Please sign in again." %>';</script>
    <script nonce="<%- nonce %>" defer src="/javascript/vendor/query-core.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/vendor/reconnecting-websocket.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/vendor/stimulus.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/vendor/turbo.js?v=1"></script>
    <!-- turbo-shell must load synchronously: inline body/page scripts call ALMount during parse -->
    <script nonce="<%- nonce %>" src="/javascript/shared/turbo-shell.js?v=2"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/format-switcher.js?v=8"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/animations.js?v=8"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/custom-select.js?v=8"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/page-loader.js?v=8"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/checkbox-anim.js?v=8"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/layout-animations.js?v=8"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/al-table.js?v=7"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/al-tabs.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/al-dialog.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/al-action.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/al-field.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/al-state.js?v=1"></script>
    <script nonce="<%- nonce %>" defer src="/javascript/shared/data-layer.js?v=1"></script>
    <style>
      * { scrollbar-width: thin; scrollbar-color: var(--theme-border) transparent; }
      ::-webkit-scrollbar { width: 8px; height: 8px; }
      ::-webkit-scrollbar-thumb { background: var(--theme-border); border-radius: 4px; }
      ::-webkit-scrollbar-track { background: transparent; }
      html, body { transition: background-color 0.25s ease-in, color 0.25s ease-in; background: var(--theme-bg); color: var(--theme-text); }
      /* Safe area handling for notched devices */
      body { 
        padding-top: env(safe-area-inset-top);
        padding-left: env(safe-area-inset-left);
        padding-right: env(safe-area-inset-right);
      }
      /* Mobile top bar spacing */
      @media (max-width: 1023px) {
        #page-content, #server-page-body { padding-top: 56px; padding-bottom: 64px; }
      }
      /* Pre-hide animatable content before JS runs so there's no flash-then-hide jitter */
      .js-loading #page-content > *:not(.mobile-top-bar):not(.mobile-bottom-nav):not(.mobile-more-sheet):not([style*="position: fixed"]),
      .js-loading #server-page-body > * {
        opacity: 0;
        transform: translateY(14px);
      }
      /* Turbo Drive's default top progress bar is hidden: the panel draws its own page-loader overlay */
      turbo-progress-bar { visibility: hidden; }
    </style>
</head>
<body style="font-family: 'General Sans';<%= (settings.panelWallpaper && !wallpaperExcluded) ? `--al-wallpaper-image:url('${settings.panelWallpaper}');` : '' %>" class="h-full<%= (settings.panelWallpaper && !wallpaperExcluded) ? ' al-wallpaper' : '' %>">
<% if (settings.panelWallpaper && !wallpaperExcluded) { %>
<!-- Panel wallpaper layer — fixed behind all surfaces. Excluded pages (Overview, Credits) never render this. -->
<div id="al-wallpaper-layer" aria-hidden="true"></div>
<% } %>

<a href="#page-content" class="sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-[99999] focus:px-4 focus:py-2 focus:rounded-lg focus:bg-neutral-900 focus:text-white dark:focus:bg-neutral-950 focus:text-sm focus:font-medium focus:shadow-lg">Skip to main content</a>

<!-- Network activity chip is created dynamically by page-loader.js -->
<script nonce="<%- nonce %>">
function initializeTheme() {
    const userPreference = localStorage.getItem('theme');
    const systemPreference = window.matchMedia('(prefers-color-scheme: dark)').matches;

    if (userPreference === 'dark' || (!userPreference && systemPreference)) {
      document.documentElement.classList.add('dark');
    } else {
      document.documentElement.classList.remove('dark');
    }
    applyThemeSheets();
  }

  function applyThemeSheets() {
    const isDark = document.documentElement.classList.contains('dark');
    const lightSheet = document.getElementById('light-theme-css');
    const darkSheet = document.getElementById('dark-theme-css');
    if (lightSheet) lightSheet.disabled = isDark;
    if (darkSheet) darkSheet.disabled = !isDark;
  }

  function toggleTheme() {
    const isDark = document.documentElement.classList.toggle('dark');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
    applyThemeSheets();
    if (typeof setTerminalTheme === 'function') {
      setTerminalTheme();
    }
    window.dispatchEvent(new CustomEvent('al:themechange'));
  }

  initializeTheme();
</script>
<%- include("./bottomNav") %>
<%- include("./modal") %>
<script nonce="<%- nonce %>" src="/javascript/shared/toast-store.js?v=1" data-turbo-eval="false"></script>
<%- include("./toast") %>
<script nonce="<%- nonce %>" src="/javascript/shared/escape.js?v=1" data-turbo-eval="false"></script>
<script nonce="<%- nonce %>" src="/javascript/shared/api.js?v=1" data-turbo-eval="false"></script>
